% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_bounding_boxes.R
\name{create_bounding_boxes}
\alias{create_bounding_boxes}
\title{Create bounding boxes for community locations}
\usage{
create_bounding_boxes(gps_df, distance_km = 5, custom_bbox = NULL)
}
\arguments{
\item{gps_df}{A data frame containing the following columns:
\describe{
\item{project}{Character; project or dataset name.}
\item{ethnicity}{Character; population or ethnic group.}
\item{community}{Character; community name.}
\item{lat}{Numeric; community latitude in decimal degrees.}
\item{lon}{Numeric; community longitude in decimal degrees.}
}}

\item{distance_km}{Numeric; the radius (in kilometers) around each central point to define the bounding box.
Defaults to 5 km.}

\item{custom_bbox}{Optional named list of bounding box coordinates, which may include either
two corners (\code{nw_lat}, \code{nw_lon}, \code{se_lat}, \code{se_lon}) or four corners
(\code{nw_lat}, \code{nw_lon}, \code{ne_lat}, \code{ne_lon}, \code{sw_lat}, \code{sw_lon}, \code{se_lat}, \code{se_lon}).}
}
\value{
A named list of bounding box objects, each containing:
\itemize{
\item \code{bbox}: Numeric vector with named elements \code{left}, \code{bottom}, \code{right}, \code{top}.
\item \code{project}, \code{ethnicity}, \code{community}: Metadata values from the input table.
}
}
\description{
Generates bounding boxes for each community in a user-provided dataset of latitude and longitude coordinates.
If specific corner coordinates are provided in \code{custom_bbox}, the function uses those directly; otherwise,
it computes a bounding box that extends a fixed distance (in kilometers) around each community's central point.
}
\examples{
\dontrun{
data <- data.frame(
  project = "CARE", ethnicity = "Qom/Toba", community = "Namqom",
  lat = -26.118, lon = -58.2254
)
bboxes <- create_bounding_boxes(data, distance_km = 10)
}

}
