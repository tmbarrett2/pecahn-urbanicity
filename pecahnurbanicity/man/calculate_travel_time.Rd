% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_travel_time.R
\name{calculate_travel_time}
\alias{calculate_travel_time}
\title{Calculate minimum travel time between points using a friction surface}
\usage{
calculate_travel_time(from_point, to_points, tr_corrected, raster_crs)
}
\arguments{
\item{from_point}{An \code{sf} point object representing the origin (typically community center).}

\item{to_points}{An \code{sf} object containing one or more destination points.}

\item{tr_corrected}{A \code{gdistance::TransitionLayer} object that has been geo-corrected.}

\item{raster_crs}{The coordinate reference system (CRS) of the friction surface raster.}
}
\value{
A numeric value giving the minimum estimated travel time (in minutes) between the
origin and destinations, or \code{NA} if unavailable.
}
\description{
Computes the minimum travel time (in minutes) from a single origin point to one or
more destination points using a precomputed, geo-corrected transition layer from
a friction surface.
}
\details{
This function transforms both origin and destination coordinates to the CRS of the
friction surface, computes least-cost path distances using \code{gdistance::costDistance()},
and returns the minimum finite value.
}
\examples{
\dontrun{
time_min <- calculate_travel_time(from_point = origin_sf,
                                  to_points = facilities_sf,
                                  tr_corrected = transition_layer,
                                  raster_crs = raster::crs(friction_raster))
}

}
